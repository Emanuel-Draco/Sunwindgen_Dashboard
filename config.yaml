name: Energy Dashboard
slug: energy_dashboard
version: "0.1.0"
description: Custom energy dashboard

arch:
  - amd64

startup: services
boot: auto
init: false

build:
  dockerfile: Dockerfile
  args:
    BUILD_FROM: ghcr.io/home-assistant/amd64-base:latest

options: {}
schema: {}

ports:
  8080/tcp: 8080




