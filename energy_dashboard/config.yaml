name: Energy Dashboard
slug: energy_dashboard
version: "0.4.2"
description: Custom standalone energy dashboard for Home Assistant

arch:
  - amd64

startup: application
boot: auto
init: false

ports:
  8080/tcp: 8080

environment:
  MOCK_MODE: "true"
